import test, { describe } from "node:test";
import { strict as assert } from "node:assert";
import { BankSystem } from "./bank.js";

describe("test the banking operations", () => {
  const name = "sample bank plc";
  const address = "Block 43, Jigawa, Dutse, Nigeria";
//   const Bank = new BankSystem(name, address);

//   test("should create a new account", () => {
//     assert.equal(Bank.address, address);
//   });
});
